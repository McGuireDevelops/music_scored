# Firebase CLI uses this instead of .gitignore for deploy uploads.
# Do not list functions/_packages/ here — vendored @learning-scores/shared must be uploaded.
# Omit blanket `lib/` so functions/lib (compiled JS) is included in the functions bundle.

node_modules/
dist/
build/
.expo/
*.local
.env
.env.local
.DS_Store
*.log
.firebase/
firebase-debug.log
*.tsbuildinfo
